# Copy this file to .env for local development (optional - falls back to
# http://localhost:9898 automatically if unset, see src/api/axiosClient.js).
#
# In Vercel, set this as a real Environment Variable in the project settings
# instead - it gets baked into the build at build time.
VITE_API_URL=http://localhost:9898
